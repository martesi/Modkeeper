- the backend will be refactored to meet its own new requirements and the new ones from the frontend. Instead of relying on the old interfaces (binding.ts), the new ui should not implement functions but use some example data.
- However, if you think it's necessary, you can now define data structure/api expected to be used/called now so that the frontend will have a easier migration. I plan to use use a sqlite db instead of separate cache/manifest file. This should be in another file.
- Do not keep all states global. It's understandable that you keep library state global, so that the user won't lose the state when switching tabs, if we implement them as routes. However I don't think all states should be managed in the global state. Ones like confirmation, should be local, maintain a direct control from parent to child. Please check the current implementation so that you have a better understanding what's needed to be local what global.
- The library x state should be written in one component with conditional displaying; however this can get monolith, thus there should be a central component for combining sub components, and major parts, like title, tool bar, execution bar, mod list, they all should be independent and handle their conditional displaying with a globar/parent state.
- Open for a path should use tauri opener plugin. Since it will be used in multiple place. A util should be created wrapping the tauri api designed for the business of the app.
- Icon browse should accept any file is displayable with img html element.
- Add to code style that centralize translation to a single util, use function to get translated content. Translation function naming should be "tText". Use namespace.
- The content of the app should have a max-width even the app can be maximized with a lot of space. When exceeded, the content of the app should be centered.
