
// This file was generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta). Do not edit this file manually.

/** user-defined commands **/


export const commands = {
async addMods(paths: string[], channel: TAURI_CHANNEL<TaskStatus>) : Promise<Result<LibraryDTO, SError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("add_mods", { paths, channel }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async removeMods(ids: string[], channel: TAURI_CHANNEL<TaskStatus>) : Promise<Result<LibraryDTO, SError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("remove_mods", { ids, channel }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async syncMods(channel: TAURI_CHANNEL<TaskStatus>) : Promise<Result<LibraryDTO, SError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("sync_mods", { channel }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getLibrary() : Promise<Result<LibraryDTO, SError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_library") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async toggleMod(id: string, isActive: boolean) : Promise<Result<LibraryDTO, SError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("toggle_mod", { id, isActive }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getBackups(modId: string) : Promise<Result<string[], SError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_backups", { modId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async restoreBackup(modId: string, timestamp: string) : Promise<Result<LibraryDTO, SError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("restore_backup", { modId, timestamp }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async openLibrary(path: string) : Promise<Result<LibrarySwitch, SError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("open_library", { path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async createLibrary(requirement: LibraryCreationRequirement) : Promise<Result<LibrarySwitch, SError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("create_library", { requirement }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
}
}

/** user-defined events **/



/** user-defined constants **/



/** user-defined types **/

export type LibraryCreationRequirement = { game_root: string; repo_root: string; name: string }
export type LibraryDTO = { id: string; name?: string; game_root: string; repo_root: string; spt_version: string; mods: Partial<{ [key in string]: Mod }>; is_dirty: boolean }
export type LibrarySwitch = { active: LibraryDTO | null; libraries: LibraryDTO[] }
export type Mod = { id: string; is_active: boolean; mod_type: ModType; name: string; manifest: ModManifest | null }
export type ModManifest = { guid: string; name: string; version: string; author: string }
export type ModType = "Client" | "Server" | "Both" | "Unknown"
export type SError = { UnsupportedSPTVersion: string } | { ParseError: string } | { IOError: string } | "GameOrServerRunning" | "ProcessRunning" | "UnableToDetermineModId" | { ModNotFound: string } | { FileOrDirectoryNotFound: string } | { FileCollision: string[] } | "Unexpected" | "Link" | { UnhandledCompression: string } | { AsyncRuntimeError: string } | "ContextUnprovided" | { UpdateStatusError: string } | "NoActiveLibrary"
export type TAURI_CHANNEL<TSend> = null
export type TaskStatus = never

/** tauri-specta globals **/

import {
	invoke as TAURI_INVOKE,
	Channel as TAURI_CHANNEL,
} from "@tauri-apps/api/core";
import * as TAURI_API_EVENT from "@tauri-apps/api/event";
import { type WebviewWindow as __WebviewWindow__ } from "@tauri-apps/api/webviewWindow";

type __EventObj__<T> = {
	listen: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.listen<T>>;
	once: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.once<T>>;
	emit: null extends T
		? (payload?: T) => ReturnType<typeof TAURI_API_EVENT.emit>
		: (payload: T) => ReturnType<typeof TAURI_API_EVENT.emit>;
};

export type Result<T, E> =
	| { status: "ok"; data: T }
	| { status: "error"; error: E };

function __makeEvents__<T extends Record<string, any>>(
	mappings: Record<keyof T, string>,
) {
	return new Proxy(
		{} as unknown as {
			[K in keyof T]: __EventObj__<T[K]> & {
				(handle: __WebviewWindow__): __EventObj__<T[K]>;
			};
		},
		{
			get: (_, event) => {
				const name = mappings[event as keyof T];

				return new Proxy((() => {}) as any, {
					apply: (_, __, [window]: [__WebviewWindow__]) => ({
						listen: (arg: any) => window.listen(name, arg),
						once: (arg: any) => window.once(name, arg),
						emit: (arg: any) => window.emit(name, arg),
					}),
					get: (_, command: keyof __EventObj__<any>) => {
						switch (command) {
							case "listen":
								return (arg: any) => TAURI_API_EVENT.listen(name, arg);
							case "once":
								return (arg: any) => TAURI_API_EVENT.once(name, arg);
							case "emit":
								return (arg: any) => TAURI_API_EVENT.emit(name, arg);
						}
					},
				});
			},
		},
	);
}
