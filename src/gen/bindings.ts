// This file was generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta). Do not edit this file manually.

/** user-defined commands **/

export const commands = {
  /**
   * Plain blocking (delta 3): commits mod state only, never deploys - no
   * symlinks touched, no collision check (C3). Sets deployStale via the dirty flag.
   */
  async bulkUpdateMods(
    input: BulkUpdateModsInput,
  ): Promise<Result<LibraryWorkspace, SError>> {
    try {
      return {
        status: 'ok',
        data: await TAURI_INVOKE('bulk_update_mods', { input }),
      }
    } catch (e) {
      if (e instanceof Error) throw e
      else return { status: 'error', error: e as any }
    }
  },
  /**
   * Fire-and-track (§7e): the Result is the accept; the outcome arrives as a
   * sync_completed event matched by taskId. Keeps the GameOrServerRunning guard
   * (delta 2, reverses C2).
   */
  async syncMods(
    input: SyncModsInput,
  ): Promise<Result<OperationAccepted, SError>> {
    try {
      return { status: 'ok', data: await TAURI_INVOKE('sync_mods', { input }) }
    } catch (e) {
      if (e instanceof Error) throw e
      else return { status: 'error', error: e as any }
    }
  },
  /**
   * Fire-and-track (§7e): outcome arrives as mod_install_completed carrying
   * per-archive failures (§7b).
   */
  async installModArchives(
    input: InstallModArchivesInput,
  ): Promise<Result<OperationAccepted, SError>> {
    try {
      return {
        status: 'ok',
        data: await TAURI_INVOKE('install_mod_archives', { input }),
      }
    } catch (e) {
      if (e instanceof Error) throw e
      else return { status: 'error', error: e as any }
    }
  },
  /**
   * Fire-and-track (§7e): outcome arrives as cache_rebuild_completed.
   * Rebuild renames only - no backup cleanup, no re-link (C8).
   */
  async rebuildLibraryCache(
    input: RebuildLibraryCacheInput,
  ): Promise<Result<OperationAccepted, SError>> {
    try {
      return {
        status: 'ok',
        data: await TAURI_INVOKE('rebuild_library_cache', { input }),
      }
    } catch (e) {
      if (e instanceof Error) throw e
      else return { status: 'error', error: e as any }
    }
  },
  async renameLibrary(
    input: RenameLibraryInput,
  ): Promise<Result<LibraryWorkspace, SError>> {
    try {
      return {
        status: 'ok',
        data: await TAURI_INVOKE('rename_library', { input }),
      }
    } catch (e) {
      if (e instanceof Error) throw e
      else return { status: 'error', error: e as any }
    }
  },
  /**
   * Two distinct destructive stakes (§7b): deleteFiles removes the registry row
   * AND the library directory (guarded by GameOrServerRunning - delta 2);
   * otherwise only the row goes, so re-adding re-adopts the id (C7).
   */
  async deleteLibrary(
    input: DeleteLibraryInput,
  ): Promise<Result<LibraryWorkspace, SError>> {
    try {
      return {
        status: 'ok',
        data: await TAURI_INVOKE('delete_library', { input }),
      }
    } catch (e) {
      if (e instanceof Error) throw e
      else return { status: 'error', error: e as any }
    }
  },
  /**
   * The startup command: returns the full workspace and, as the first frontend
   * call, carries the watchdog handoff (C11) and shows the window (prevents an
   * unstyled flash). Also delivers any pending startup config warning once (C12).
   */
  async getLibraryWorkspace(): Promise<Result<LibraryWorkspace, SError>> {
    try {
      return { status: 'ok', data: await TAURI_INVOKE('get_library_workspace') }
    } catch (e) {
      if (e instanceof Error) throw e
      else return { status: 'error', error: e as any }
    }
  },
  async activateLibrary(
    input: ActivateLibraryInput,
  ): Promise<Result<LibraryWorkspace, SError>> {
    try {
      return {
        status: 'ok',
        data: await TAURI_INVOKE('activate_library', { input }),
      }
    } catch (e) {
      if (e instanceof Error) throw e
      else return { status: 'error', error: e as any }
    }
  },
  async createLibrary(
    input: CreateLibraryInput,
  ): Promise<Result<LibraryWorkspace, SError>> {
    try {
      return {
        status: 'ok',
        data: await TAURI_INVOKE('create_library', { input }),
      }
    } catch (e) {
      if (e instanceof Error) throw e
      else return { status: 'error', error: e as any }
    }
  },
  /**
   * Apply window vibrancy effect based on OS and theme
   * - Windows 11: Mica effect with dark mode support
   * - Windows 10: Acrylic effect with tint color
   * - macOS: Vibrancy effect (follows system appearance)
   */
  async applyWindowEffect(isDark: boolean | null): Promise<void> {
    await TAURI_INVOKE('apply_window_effect', { isDark })
  },
  async getSettings(): Promise<Result<AppSettings, SError>> {
    try {
      return { status: 'ok', data: await TAURI_INVOKE('get_settings') }
    } catch (e) {
      if (e instanceof Error) throw e
      else return { status: 'error', error: e as any }
    }
  },
  async saveSettings(
    settings: AppSettings,
  ): Promise<Result<AppSettings, SError>> {
    try {
      return {
        status: 'ok',
        data: await TAURI_INVOKE('save_settings', { settings }),
      }
    } catch (e) {
      if (e instanceof Error) throw e
      else return { status: 'error', error: e as any }
    }
  },
}

/** user-defined events **/

export const events = __makeEvents__<{
  workspaceEvent: WorkspaceEvent
}>({
  workspaceEvent: 'workspace-event',
})

/** user-defined constants **/

/** user-defined types **/

export type ActivateLibraryInput = { libraryId: string }
export type AppSettings = {
  theme: ThemeMode
  accentColor: string
  language: string
}
export type ArchiveFailure = { archivePath: string; error: SError }
export type BulkModAction = 'enable' | 'disable' | 'delete'
export type BulkUpdateModsInput = {
  libraryId: string
  modIds: string[]
  action: BulkModAction
}
export type CacheState = 'ready' | 'dirty' | 'rebuilding' | 'failed'
export type CacheStatus = {
  state: CacheState
  message?: string | null
  lastRebuiltAt?: string | null
}
export type CreateLibraryInput = {
  gameRoot: string
  libraryRoot: string | null
  name: string | null
}
export type DeleteLibraryInput = { libraryId: string; deleteFiles: boolean }
export type InstallModArchivesInput = {
  taskId: string
  libraryId: string
  archivePaths: string[]
}
/**
 * A registered library is either readable (summary) or a path-only stub -
 * never dropped from the list (C13).
 */
export type LibraryEntry = LibrarySummary | LibraryStub
/**
 * Registered-but-unreadable; render the bare path, remove-only.
 */
export type LibraryStub = { path: string }
export type LibrarySummary = {
  id: string
  name: string
  gameRoot: string
  libraryRoot: string
  sptVersion?: string | null
  cacheStatus: CacheStatus
  /**
   * Deployed symlinks no longer match recorded state; drives the Sync highlight (C3).
   */
  deployStale: boolean
  updatedAt: string
}
/**
 * The full frontend-facing state: every registered library, its mods,
 * and the app settings. Assembled read-only (C13).
 */
export type LibraryWorkspace = {
  activeLibraryId?: string | null
  libraries: LibraryEntry[]
  modsByLibraryId: Partial<{ [key in string]: ModSummary[] }>
  /**
   * Present-but-empty this pass; the tool registry is deferred (§7d).
   */
  toolsByLibraryId: Partial<{ [key in string]: ToolSummary[] }>
  settings: AppSettings
  /**
   * One-shot startup config problem for the frontend to toast (C12).
   */
  configWarning?: string | null
}
export type ModFailure = { modId: string; error: SError }
export type ModSummary = {
  id: string
  libraryId: string
  name: string
  type: ModType
  isEnabled: boolean
  sourcePath?: string | null
  installedPath?: string | null
  updatedAt: string
}
export type ModType = 'client' | 'server' | 'both' | 'unknown'
/**
 * Fire-and-track accept signal: sync validation passed, outcome arrives
 * as a WorkspaceEvent matched by taskId (§7e).
 */
export type OperationAccepted = { accepted: boolean }
export type RebuildLibraryCacheInput = { taskId: string; libraryId: string }
export type RenameLibraryInput = { libraryId: string; name: string }
/**
 * Serializes to a stable `{ code, data }` wire shape; `code` is the variant
 * name verbatim (generated by the serde attribute - no mapping table to
 * drift). Display is developer-log-only and never crosses the wire (§7g).
 */
export type SError =
  | { code: 'UnsupportedSPTVersion'; data: string }
  | { code: 'ParseError'; data: string }
  | { code: 'IOError'; data: string }
  | { code: 'GameOrServerRunning' }
  | { code: 'ProcessRunning' }
  | { code: 'UnableToDetermineModId' }
  | { code: 'ModNotFound'; data: string }
  | { code: 'FileOrDirectoryNotFound'; data: string }
  | { code: 'FileCollision'; data: string[] }
  | { code: 'Unexpected' }
  | { code: 'UnhandledCompression'; data: string }
  | { code: 'AsyncRuntimeError'; data: string }
  | { code: 'NoActiveLibrary' }
  | { code: 'InvalidLibrary'; data: [string, string] }
  | { code: 'ModIdConflict'; data: [string, string] }
  | { code: 'ConfigSaveFailed'; data: string }
  | { code: 'TaskIdInUse'; data: string }
export type SyncModsInput = { taskId: string; libraryId: string }
export type ThemeMode = 'system' | 'light' | 'dark'
/**
 * Defined for forward-compat; unused by live commands this pass (§7d).
 */
export type ToolSummary = {
  id: string
  libraryId: string
  name: string
  executablePath: string
  iconDataUrl?: string | null
  launchArgs?: string | null
  updatedAt: string
}
/**
 * Completion events for the three fire-and-track operations (§7e/§9).
 */
export type WorkspaceEvent =
  | {
      type: 'cache_rebuild_completed'
      taskId: string
      libraryId: string
      cacheStatus: CacheStatus
      workspace: LibraryWorkspace
    }
  | {
      type: 'mod_install_completed'
      taskId: string
      libraryId: string
      failures: ArchiveFailure[]
      workspace: LibraryWorkspace
    }
  | {
      type: 'sync_completed'
      taskId: string
      libraryId: string
      failures: ModFailure[]
      workspace: LibraryWorkspace
    }

/** tauri-specta globals **/

import {
  invoke as TAURI_INVOKE,
  Channel as TAURI_CHANNEL,
} from '@tauri-apps/api/core'
import * as TAURI_API_EVENT from '@tauri-apps/api/event'
import { type WebviewWindow as __WebviewWindow__ } from '@tauri-apps/api/webviewWindow'

type __EventObj__<T> = {
  listen: (
    cb: TAURI_API_EVENT.EventCallback<T>,
  ) => ReturnType<typeof TAURI_API_EVENT.listen<T>>
  once: (
    cb: TAURI_API_EVENT.EventCallback<T>,
  ) => ReturnType<typeof TAURI_API_EVENT.once<T>>
  emit: null extends T
    ? (payload?: T) => ReturnType<typeof TAURI_API_EVENT.emit>
    : (payload: T) => ReturnType<typeof TAURI_API_EVENT.emit>
}

export type Result<T, E> =
  | { status: 'ok'; data: T }
  | { status: 'error'; error: E }

function __makeEvents__<T extends Record<string, any>>(
  mappings: Record<keyof T, string>,
) {
  return new Proxy(
    {} as unknown as {
      [K in keyof T]: __EventObj__<T[K]> & {
        (handle: __WebviewWindow__): __EventObj__<T[K]>
      }
    },
    {
      get: (_, event) => {
        const name = mappings[event as keyof T]

        return new Proxy((() => {}) as any, {
          apply: (_, __, [window]: [__WebviewWindow__]) => ({
            listen: (arg: any) => window.listen(name, arg),
            once: (arg: any) => window.once(name, arg),
            emit: (arg: any) => window.emit(name, arg),
          }),
          get: (_, command: keyof __EventObj__<any>) => {
            switch (command) {
              case 'listen':
                return (arg: any) => TAURI_API_EVENT.listen(name, arg)
              case 'once':
                return (arg: any) => TAURI_API_EVENT.once(name, arg)
              case 'emit':
                return (arg: any) => TAURI_API_EVENT.emit(name, arg)
            }
          },
        })
      },
    },
  )
}
